---
description: 代码变更后重启 life-sim 前后端服务
alwaysApply: true
---

# 变更后重启服务

在本项目中完成代码修改后，**必须重启相关服务**，确保改动生效：

- **后端**（`backend/` 下 Go 代码、prompts、配置）：重启 `go run .`，监听 `:8080`
- **前端**（`frontend/` 下 Vue/TS 代码）：重启 `npm run dev`，监听 `:5173`

## 重启方式（Windows）

```powershell
# 后端（项目根目录，读取 .env）
cd backend
# 或运行 run-backend.bat

# 前端
cd frontend
npm run dev
```

## 执行时机

- 每次 assistant 完成涉及后端或前端的代码变更后，在回复用户前主动重启对应服务
- 若端口被占用，先结束占用 `:8080` / `:5173` 的旧进程再启动
- 后端依赖 `DEEPSEEK_API_KEY`（来自 `.env`），勿提交 `.env` 到 Git
